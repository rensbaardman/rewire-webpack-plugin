// This is a sanity check to make sure all dependencies
// have been declared properly, and installation
// ('npm install rewire-webpack-plugin') goes smoothly.

const assert = require('chai').assert;

describe('RewireWebpackPlugin', () => {

	it('should install without warnings', () => {
		assert.fail('finish the test')
		// see e.g. https://docs.npmjs.com/cli/link on how
		// to install a module locally
	})

})
